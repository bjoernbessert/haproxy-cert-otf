language: minimal

sudo: 'required'

services:
  - docker

dist: xenial

before_install:
  - sudo apt-get update && sudo apt-get -y install bats

install:
  - cd dockerfiles/apache && make build-without-cache && cd ../../
  - cd dockerfiles/apache && make build-without-cache && cd ../../

script:
  - make test

after_script:
  - make test

